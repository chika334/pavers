import React, { useState } from 'react';

const Transactions = () => {
  const [modelState, setModelState] = useState({
    property: 'value',
  });
  return (
    <div>
      Transactions
    </div>
  );
};

export default Transactions;